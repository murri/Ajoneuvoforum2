<h1>ilmoitukset</h1>

<table>
  <tr>
    <th>Tyyppi</th>
    <th>Merkki</th>
    <th>Malli</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ilmoitus.each do |ilmoitu| %>
  <tr>
    <td><%= ilmoitu.tyyppi %></td>
    <td><%= ilmoitu.merkki %></td>
    <td><%= ilmoitu.malli %></td>
    <td><%= link_to 'Show', ilmoitu %></td>
    <td><%= link_to 'Edit', edit_ilmoitu_path(ilmoitu) %></td>
    <td><%= link_to 'Destroy', ilmoitu, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Uusi ilmoitus', new_ilmoitu_path %>
